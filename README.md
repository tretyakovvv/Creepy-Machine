# Creepy-Machine
# Creepy-Machine

## Railway

If you deploy this app on Railway, set `SITE_URL` to your public Railway domain
for example `https://your-service.up.railway.app`.

Do not leave `SITE_URL` pointing to `http://localhost:3000` in production.
